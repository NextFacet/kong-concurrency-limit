## What changed and why

## Testing

- [ ] `./scripts/test-lifecycle.sh` passes
- [ ] For changes to the plugin's concurrency logic: ran a validation soak
      (`python3 scripts/soak_test.py --validation ...`)
- [ ] `CHANGELOG.md` updated (for user-facing changes)
